// What value does the following expression evaluate to?

parseInt('3.1415');

// Solution: 3. parseInt is explicit type coercision of string to integer.
// It evalutes to 3. parseInt converts strings that being with a digit to
// their numeric equivalent. If it encounters a non-digit such as ., it stops
// converting. Thus, this example converts the 3 at the beginning of the string
// but ignores everything else.