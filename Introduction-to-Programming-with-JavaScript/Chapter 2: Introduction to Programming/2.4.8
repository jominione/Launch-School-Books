// Without running the following code, what do you think it will output?

function foo(bar, qux) {
  console.log(bar);
  console.log(qux);
}

foo(42, 3.1415, 2.718);

// Solution: Will log 42 and 3.1415. JavaScript will ignore additional 
// arguments if too many arguments are provided when the calling a function.