// Write a function that takes a string an as argument and returns an all-caps
// version of the string when the string is longer than 10 characters. 
// Otherwise, it should return the original string. Example: change 
// 'hello world' to 'HELLO WORLD', but don't change 'goodbye'.

// Solution:

function capitalise(text) {
  return (text.length > 10 ? text.toUpperCase() : text);
}

console.log(capitalise("Sue Smith"));     // => Sue Smith
console.log(capitalise("Sue Robertson")); // => SUE ROBERTSON
console.log(capitalise("Joe Thomas"));    // => Joe Thomas
console.log(capitalise("Joe Stevens"));   // => JOE STEVENS