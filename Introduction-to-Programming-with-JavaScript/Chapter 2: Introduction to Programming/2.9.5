// What does the following function do?

function doSomething(string) {
  return string.split(' ').reverse().map((value) => value.length);
}

// Solution. Takes string argument and split delimited by ' ' into array.
// Order of elements in array reversed and then new array returned which
// is length of each element.