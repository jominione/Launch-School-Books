// Add a qux property with value 3 to the myObj object we created in the
// previous exercise. 

let myProtoObj = {
  foo: 1,
  bar: 2,
};

let myObj = Object.create(myProtoObj);
myObj.qux = 3;

// Now examine the following code snippets:

let objKeys = Object.keys(myObj);
objKeys.forEach(function(key) {
  console.log(key);
});

for (let key in myObj) {
  console.log(key);
}

// Without running this code, can you determine whether these two snippets
// produce the same output? Why?

// Solution [Wrong]: No, they will not produce the same output. The first 
// snippet will log the keys of myObj to console while the second snippet 
// will log the elements of the myObj to the console.

// Both snippets iterate over the keys of myObj. However, for..in iterates 
// over all of the object's keys, including those in the prototype object, 
// myProtoObj. Thus, snippet 2 logs:

// qux
// foo
// bar

// Snippet 1 iterates solely over myObj's "own" properties - that is, those 
// defined directly on the object, not its prototype. Thus, it logs:

// qux