// Which of the following values are valid keys for an object?

// Solution:

// * 1 - Valid
// * '1' - Valid
// * undefined - Valid
// * 'hello world' - Valid
// * true - Valid
// * 'true' - Valid

// All the listed values are valid keys. Note, though, that JavaScript coerces
// the non-string key values to strings. Given the listed values, 1 and '1'
// represent the same key, as do true and 'true'. This equivalency will bite
// you at some point.