-- Make sure you are connected to the encyclopedia database. Add a column 
-- to the animals table called class to hold strings of up to 100 characters.

-- Update all the rows in the table so that this column holds the value Aves.

-- Solution:

\c encyclopedia

ALTER TABLE animals
ADD COLUMN class varchar(100);

UPDATE animals SET class = 'Aves';